# Chantiers

> Une ligne par chantier. Créé par le DG à l'issue d'un comité ou d'un briefing.
>
> **Statut** : `todo` · `doing` · `blocked` · `review` · `done`
> **Prio** : `P1` ce trimestre · `P2` utile · `P3` un jour
> **Auto** : `oui` seulement si une routine planifiée peut l'exécuter sans humain
> devant l'écran. Par défaut `non` — ne jamais passer à `oui` de sa propre initiative.

> **Amorce du 2026-09-22.** Aucune ligne n'est devinée. C1–C17 viennent des sections 3 et 4
> de `HANDOFF.md` ; C18–C22 de la relecture des notes de mémoire du projet, qui portaient
> des chantiers ouverts que le handoff n'avait jamais repris — `VITE_BOOKING_URL` vide
> depuis toujours, le double comptage GA4 ouvert depuis le 13/08, le cloisonnement
> OpenRouter laissé en plan le 17/09, l'épuisement du pool de sujets et deux workflows
> jamais inspectés. Les chantiers que le handoff donne pour clos (tâches CRM, vitrine des
> réalisations, montées de dépendances, traces de l'offre abandonnée dans ce dépôt) n'y
> figurent pas. `grep TODO/FIXME` sur `src/`, `scripts/` et `docker/backend/` hors
> `node_modules` : **aucun résultat**.

## Ouverts

| ID | Chantier | Responsable | Prio | Statut | Échéance | Auto |
|----|----------|-------------|------|--------|----------|------|
| C1c | **Runner de tâches externe — analysé le 24/09, délibérément différé.** C'est la dernière dépréciation n8n, et la seule qui ne soit pas une ligne de configuration : il faut lancer le *task runner launcher* comme processus séparé et partager `N8N_RUNNERS_AUTH_TOKEN`. **Le coût réel est ailleurs** : le runner externe est un autre processus, avec son propre environnement, et **8 workflows actifs lisent `$env`** — dont l'auto-blog (`OPENROUTER_API_KEY`) et la génération de playlists. Basculer sans reprendre l'environnement du launcher casserait tout ce qui dépend de `$env`, en silence. À faire quand la v3 aura une date : l'image étant épinglée depuis le 23/09, c'est nous qui choisissons le moment. *(La migration du stockage, elle, est faite — C1d.)* | Dev | P3 | todo | — | non |
| C39 | **Inventaire fait le 25/09 — la bascule est sans risque, elle attend ton feu vert.** 44 conteneurs actifs passes au crible, par label. **La categorie qui casserait est vide** : aucun conteneur ne declare un routeur Traefik sans declarer aussi `traefik.enable=true`. **22** sont declares et surs, **3** deja explicitement hors champ (`traefik.enable=false`), et **19** sont ramasses par defaut sans aucun label — bases PostgreSQL, Redis, pgbouncer, un Adminer, et Traefik lui-meme. Poser `--providers.docker.exposedByDefault=false` sortirait ces 19 du champ, ce qui est exactement le but. Cout : une recreation de Traefik, soit quelques secondes d'interruption sur **tous** les sites. | Dev | P2 | review | — | non |
| C40 | **Un jeton d'autorisation en clair dans la commande d'un conteneur.** Le service `cron` de `/docker/dreamoracle/docker-compose.yml` construit une crontab qui appelle une route interne avec un `Authorization: Bearer` **ecrit en dur dans la commande**. Consequence : la valeur est lisible par `docker inspect`, apparait dans la table des processus du conteneur, et vit dans un fichier de compose. Baseline Distr'Action, *Secrets et acces* : aucun secret en clair dans un fichier, un log ou une commande. A remplacer par une variable d'environnement lue depuis le `.env` du projet. **Valeur jamais affichee ni recopiee** — ni ici, ni dans le journal. **Depot DreamOracle**, donc a faire depuis une session DreamOracle. | Dev | P2 | todo | — | non |
| C41 | **Trois services sortis de `0.0.0.0` le 25/09 ; le reste attend ta main.** **Fait** : `audityo-postgres` (**production**), `audityo-dev-postgres` et `radio_morning_adminer` — une console d'administration de base de donnees — sont desormais lies a `127.0.0.1`, comme les onze autres bases du VPS. Le port hote reste, pour `pg_dump` et l'outillage local ; il cesse seulement d'ecouter sur toutes les interfaces. Verifie depuis Internet : 5442, 5443 et 8090 sont fermes, les quatre sites repondent 200, et le blog d'Audityo lit bien sa base (18 articles) apres le redemarrage de la production. **Reste deux choses, et elles sont pour Laurent.** (1) **Les regles de pare-feu qui ouvrent sur du vide** — 2022, 8000-8050, 32768, et desormais 5000 : le classificateur refuse que je supprime une regle de pare-feu, motif « Security Weaken », sans distinguer qu'en retirer une *permissive* renforce. Groupe **243062**, regles **838233**, **838234**, **874653**, **889597** ; ne toucher ni a 838211 (SSH 22), ni a 838231 (80), ni a 838232 (443). (2) **UFW reste inactif.** A poser, mais sans illusion : **Docker contourne UFW** en ecrivant directement dans iptables, donc un port publie par un conteneur reste ouvert malgre `deny incoming`. UFW protegerait les services de l'hote, pas les conteneurs — la vraie protection de ceux-ci est la liaison a `127.0.0.1`, qui est faite. | Dev | P2 | review | — | non |
| C43 | **`scraping-belgique` telecharge son code au demarrage depuis un hebergeur anonyme.** Son compose fait `curl -L -o /tmp/app.tar.gz 'https://files.catbox.moe/aeqa7h.gz'` puis l'extrait et l'execute. Trois consequences : **qui controle ce fichier controle le code qui tourne sur le VPS** ; le fichier peut disparaitre a tout moment, et le conteneur ne redemarrerait plus (verifie le 25/09, il repond encore, 213 Ko) ; et **le code source n'est dans aucun depot**. C'est exactement la chaine d'approvisionnement que la baseline demande de maitriser. A remplacer par une image construite depuis un depot, ou au minimum par une archive versionnee et verifiee par empreinte. | Dev | P2 | todo | — | non |
| C44 | **Paperclip tourne de nouveau depuis le 22/09, alors que la note le donne arrete.** `paperclip-zjyk-paperclip-1` a demarre le **22/09 a 12h00**, et le VPS n'a pas redemarre depuis huit jours : ce n'est donc pas la politique `unless-stopped` au reboot, c'est un `up` lance par quelqu'un ou quelque chose. Il avait ete arrete le 17/09 parce qu'il tournait a vide pour ~21 $/mois. Point rassurant : sa cle OpenRouter n'a **pas servi depuis huit jours** — il consomme du CPU et de la memoire, pas des credits. A trancher : le rearreter, ou assumer qu'il tourne. Et corriger la note de memoire, qui affirme qu'il ne redemarrerait pas. | Dev | P3 | todo | — | non |
| C2 | Passer ce dépôt GitHub en **privé** (constaté public le 19/09). ⚠️ **Ce n'est pas un clic, et le faire tel quel casse la production.** Vérifié le 23/09 : le compose de `/docker/ainspiration` télécharge au démarrage, depuis `raw.githubusercontent.com/laurent7850/AInspiration/main`, le backend, `files.txt`, `init.sql` et les **sept migrations**. Sur un dépôt privé ces URL renvoient 404 sans jeton — **le conteneur ne démarrerait plus au prochain recreate**, c'est-à-dire au prochain déploiement. Prérequis à trancher d'abord : authentifier les `curl` avec un jeton (un secret de plus à gérer, et dans le compose), ou cesser de télécharger au boot (backend embarqué dans une image). | Dev | P1 | blocked | — | non |
| C6 | Prouver que le relais du formulaire Audityo arrive bien dans la boîte : il faut une soumission réelle **postérieure au 19/09**. Contrôle hebdomadaire conçu, prérequis manquant = un token de l'API Email d'`audityo.eu` en credential n8n, **à créer par Laurent dans hPanel**. | Opérations | P2 | blocked | — | non |
| C7 | **Trace des succes activee le 24/09 — les gardes restent desarmes, et c'est delibere.** `saveDataSuccessExecution` passe de `"none"` a `"all"` sur les deux workflows Audityo (`cb7vwsHOU4smftxp`, `QGteQI3hwH1h6uDd`), verifie sur la version publiee. Purge n8n aux valeurs par defaut — 14 jours, 10 000 executions — donc la retention des adresses e-mail est bornee. **Le correctif ne peut pas aller plus loin depuis ce depot.** Les deux `IF` portent `typeVersion: 2` sans `conditions.options.version: 2` et laissent tout passer ; mais les deux workflows ont **zero execution enregistree**, donc ni la forme du corps poste (`$json.body.email`) ni le nombre de segments du jeton ne sont verifiables. La regex du jeton en exige **deux**, un JWT en a **trois** : armer sans corriger couperait la reinitialisation en production, corriger sur supposition n'est pas mieux. **La forme du jeton vit dans le depot Audityo**, qui ne se lit pas depuis une session AInspiration. Suite : une session Audityo, apres une inscription et une reinitialisation reelles. Correctif a appliquer alors, ensemble : `^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$` **et** `options.version: 2`. | Dev | P2 | blocked | — | non |
| C47 | **Audityo audite en production le 26/09 — une erreur de DROIT sur l'argument central, et deux defauts de referencement.** L'accueil annonce, avant 35 / 15 / 7,5 millions d'euros : *« le montant retenu est **le plus eleve** des deux : la somme absolue, ou le pourcentage du chiffre d'affaires mondial »*. C'est le regime general, exact pour une grande entreprise — mais le titre de la page est *« Conformite EU AI Act pour les **PME** europeennes »*, et pour une PME **l'article 99 § 6 retient le plus BAS des deux** (verifie sur `ai-act-service-desk.ec.europa.eu`, le service officiel de la Commission, et concordant sur quatre sources juridiques). Pour une PME a 2 M€ de chiffre d'affaires, le plafond de non-conformite est **60 000 €, pas 15 M€** : la page affiche une exposition fausse d'un facteur ~250 a son propre public, et ca se lit comme de la vente par la peur. Sur un produit de conformite, c'est la premiere chose qu'un juriste verifiera. **Corrige d'ici : le prompt du chatbot** (`rdqUmTQThti94POA`) portait la meme phrase, recopiee de l'accueil le 25/09 ; n8n n'etant pas un depot voisin, il a ete corrige et teste deux fois en production — il donne le regime general puis la regle PME, et refuse de chiffrer un cas. **Le site, lui, ne se touche pas d'ici** (une session par depot) : consigne prete a coller dans `docs/chantiers/audityo-irreprochable.md`, avec la formulation proposee. **Deux autres defauts** : le sitemap ne porte que **10 URLs et aucun des 18 articles** du blog (ils sont pourtant bien rendus cote serveur, ~8 300 caracteres en HTML brut — ils ne sont pas annonces, contrairement aux 88 URLs d'AInspiration) ; et il annonce `/auth/login` et `/auth/register` qui portent `noindex`, ce que Search Console remonte en erreur. **Verifie et juste, a ne pas defaire** : le calendrier avec le report de l'annexe III au 2 decembre 2027 (confirme sur `digital-strategy.ec.europa.eu`), les quatre categories et leurs references, les trois montants de l'article 99, la TVA BE 0462.122.648, et **aucune preuve fabriquee**. Mineur : toute route racine inconnue rend 307 vers `/auth/login` au lieu de 404, mais le `noindex` + un `canonical` sans query string neutralisent deja la duplication. | Dev | P1 | review | — | non |
| C10 | **Le gros est fait le 26/09 — il ne reste qu'un arbitrage, et il est pour toi.** Tout ce qui etait *faux* est corrige (non deploye a cette heure) : le `24h` que `AnimatedStats` affichait **sous le libelle « Pour en parler trente minutes »** — le libelle avait ete reecrit, le chiffre non ; le `48h` « Mise en place type » de `AnimatedStats` et de `Consulting.tsx`, remplace par **14 realisations en service**, un fait verifiable et deja affiche ailleurs ; le `48h / Premiere video` de `video.json` (x3) ; le `-80% / Couts vs tournage` recadre en **objectif**, sur le modele du `-60%` existant ; et le « premier resultat concret en 5 jours » de la meta de `/solutions`, **en double front et backend** (`seoConfig.ts` et `routes/seo.js`). **Trouve en chemin, et c'etait le plus grave :** l'offre d'**audit gratuit abandonnee le 16/09** etait encore publiee aux moteurs. La FAQ visible etait propre dans les trois langues, mais `HomePage.tsx` portait un **second jeu de questions en dur**, en francais seulement, donne a `getFAQSchema()` — donc en donnees structurees JSON-LD. Un lecteur voyait « Le rendez-vous engage-t-il a quelque chose ? / Non », Google recevait « L'audit est-il vraiment gratuit ? / Oui, 100% gratuit ». Meme texte duplique dans le repli de `FAQ.tsx`. **La duplication etait la cause**, pas le texte : les deux copies en dur n'avaient pas suivi la correction des locales. Source unique posee — `src/data/faq.ts`, **generee depuis `public/locales/fr/common.json`**, importee par les deux. Meme defaut sur `/transformation` : sa FAQ visible *et* son `getServiceSchema` promettaient « premiers resultats en 5 jours » — corriges. Typecheck, lint et 84 tests verts. **Ce qui reste a trancher :** deux promesses de **delai de reponse**, d'une autre nature que les delais de livraison ci-dessus — `local.json hero.ctaSub` « Reponse sous 24h » sur `/pme-hainaut-bruxelles` (les trois langues) et `forms.json` « Je reponds sous 24 heures ouvrees (lundi-vendredi, 9h-18h) » (FR seulement). C'est un engagement personnel tenable, pas un chiffre invente : **a garder si tu le tiens, a retirer sinon.** Je ne tranche pas a ta place. | Marketing | P2 | review | — | non |
| C46 | **Trois fichiers de l'offre d'audit gratuit abandonnee sont encore dans le depot, importes par personne.** `src/services/auditLiveProtocol.ts`, `src/services/auditReportTemplate.ts` et `src/services/postAuditEmails.ts` — verifie le 26/09 : **aucun `import` ne les cite**, ils sont donc elimines au build et **ne sont pas servis**. Ce n'est pas un defaut de verite en production, c'est du poids mort qui porte l'offre retiree le 16/09 : delais « livre en 5 jours ouvres », sequences d'e-mails post-audit, gabarits de rapport. Le risque reel est qu'on les rebranche un jour en croyant qu'ils decrivent l'offre courante — c'est exactement ce qui vient d'arriver avec les deux copies en dur de la FAQ (C10). **A supprimer**, apres un dernier `grep` de chaque nom exporte ; ou a garder en archive hors `src/` si quelque chose y merite d'etre relu avant. Trouve en balayant tout `src/` plutot que les seuls dossiers de contenu — mon premier balayage etait trop etroit et les avait manques. | Dev | P3 | todo | — | non |
| C11 | Un outil par métier pour la communication multicanale. **Dans l'ordre, rien avant** : (1) Laurent demande l'accès Community Management à LinkedIn ; (2) session CommunityOS pour obtenir **une** publication réelle ; (3) ne pas toucher aux auto-blogs avant fin novembre ; (4) retirer le module LinkedIn du CRM, mais seulement après (2). Consignes dans `docs/chantiers/communication-multicanal.md`. | DG | P1 | todo | — | non |
| C12 | Relire le **DPA v1.0** — toujours référencé vivant par les CGV §12, jamais relu depuis avril — et retirer du `CLAUDE.md` de Paperclip la grille « Comité #2 » (Pack Express, audit gratuit, 290 €/mois) qui nourrit encore les dix agents. **Hors dépôt** : consigne prête dans `docs/chantiers/sla-obsolete-paperclip.md`, à exécuter depuis une session Paperclip. | Légal | P2 | todo | — | non |
| C13 | Remplir le CRM en prospects réels. L'ingestion fonctionne depuis le 15/09 ; il n'y a **aucun prospect réel**. Relève du GTM LinkedIn sur la grille O1–O5, pas du code — rien à faire côté dépôt tant que le flux entrant n'existe pas. | Ventes | P1 | todo | — | non |
| C20 | **Cloisonnement termine le 25/09 — `Maudios` est libre.** Les **sept** projets tournent chacun sur leur propre cle plafonnee (11 $/jour d'exposition maximale au lieu d'une cle maitresse illimitee), empreinte du conteneur confrontee a celle du fichier a chaque fois, jamais une valeur affichee. Plus **aucun conteneur** ne porte `Maudios`. Les sept plafonds sont **journaliers**, verifie par `GET /api/v1/key` (`limit_reset`) — ils avaient tous ete crees a vie. **Il reste a revoquer, et pas avant le 27/09** : releve de reference prise le 25/09 a midi — `Maudios` a **0,00 $ aujourd'hui**, 0,29 $ sur la semaine (trafic anterieur au cloisonnement). La regle tient : 24 a 48 h de `usage_daily` a zero avant suppression, c'est la seule preuve que plus rien ne l'utilise. Meme prudence ensuite pour `Seo` (3 conteneurs + le shell de Laurent) et `Oracle` (2). `distraction2026` est identifiee : credential n8n, seconde cle maitresse, voir **C31**. [`docs/chantiers/openrouter-cloisonnement.md`](../docs/chantiers/openrouter-cloisonnement.md). | Dev | P2 | review | 2026-09-27 | non |
| C21 | **Échéance recalculée le 24/09 : juillet 2027, pas février.** Mesuré en rejouant la logique de `Choisir le sujet` contre les 32 titres FR publiés : **20 sujets sur 22 sont libres**, soit 40 semaines à raison d'une semaine sur deux. Le « février 2027 » datait d'avant le correctif du 21/09, quand la moitié du pool était inatteignable — le correctif a repoussé l'échéance de cinq mois et personne n'avait refait le calcul. Les deux sujets couverts sont le Hainaut (jaccard 1.00) et les tâches administratives (0.57). **Ne pas renouveler le pool avant la décision de fin novembre** ([ADR-003](decisions/ADR-003-blog-trilingue-fenetre-novembre.md)) : écrire vingt sujets qu'on jetterait peut-être deux mois plus tard n'a pas de sens. | Marketing | P3 | todo | 2027-05 | non |
| C17 | Trancher le sort de la newsletter : suppression définitive ou relance. Tables, routes et pages conservées volontairement — **un abonné en attente existe et ses droits RGPD doivent rester servis**. Ne rien supprimer sans arbitrage. | DG | P3 | todo | — | non |

## Clos récemment

> Les chantiers `done` restent ici trois mois, puis se purgent. Le journal garde la trace.

| ID | Chantier | Responsable | Clos le |
|----|----------|-------------|---------|
| C45 | **Les alertes « dockerd emballe » venaient de mon propre processus oublie — clos 26/09.** Un `docker logs scraping-belgique-web-1 --tail 3` lance a **11:02:38** le 25/09 par une de mes commandes de diagnostic ne rendait pas la main. Un client de log accroche fait tourner le suiveur de `dockerd` en boucle : **109 075 `pread64` en 8 s**, soit ~13 600/s sur le fichier de log du conteneur. Le demon a brule **1 a 2 coeurs pendant 17 heures** (mesure a 193 % au moment du kill, 21 h 44 de CPU cumule), et le watchdog a envoye trois alertes. Premier depassement de seuil a **11:07:49**, cinq minutes apres ma commande : la correlation est sans ambiguite. **La suppression du fichier de log n'etait pas la cause** — le conteneur n'a ete recree qu'a 14:00, pres de trois heures apres le debut de la flambee (correctif C42) ; elle a seulement ajoute le `(deleted)` qui a rendu le symptome lisible. **Pourquoi `docker logs --tail 3` s'est bloque n'est PAS etabli** : rejoue ce matin, il rend la main en 0 s ; le conteneur n'a ni `tty` ni `stdin_open` ; la rotation est armee (10 Mo x 3) et un suiveur present pendant une rotation est un declencheur connu chez Docker, sans preuve ici. **Corrige par `kill` du client, sans redemarrer le demon** : `dockerd` de 104-193 % a **1,7-16,6 %**, `pread64` de 109 075/8 s a **7/5 s**, descripteurs supprimes de 1 a **0**, charge de 1,85 a **0,37**. Les quatre sites repondent ; le watchdog a confirme de lui-meme le retour a la normale a 04:52. **Garde-fou pose** : `/opt/vps-watchdog.sh` gagne `contexte_dockerd`, appelee seulement quand le processus emballe est `dockerd`, qui nomme dans le mail les clients `docker logs`/`attach`/`events`/`stats`/`wait` accroches depuis plus de 5 min et le nombre de descripteurs supprimes, avec la consigne de tuer le client et de **ne pas** redemarrer le demon ; quand aucun client n'est accroche elle renvoie vers la piste containerd du 17/09, pertinente dans ce seul cas. Filtre discriminant sur `ps -o comm` (`docker` pour le client, `bash` pour son shell) sans quoi le diagnostic se denonce lui-meme. Les deux branches testees. Miroir dans `docs/ops/vps/vps-watchdog.sh`. **Lecon** : toute commande de diagnostic appelant `docker logs` se borne par `timeout`. | Dev | 2026-09-26 |
| C1b | **Dépréciations n8n traitées.** Quatre des six annoncées au boot : `WEBHOOK_URL` → `N8N_WEBHOOK_URL`, et trois plafonds figés à leur valeur actuelle pour qu'une montée en v3 ne change pas le comportement en silence (timeout de tâche, décompression, entrées de zip). `N8N_UNVERIFIED_PACKAGES_ENABLED` posé à `false` — le défaut y va, autant le choisir. Retiré au passage : les trois `N8N_BASIC_AUTH_*`, **mortes depuis n8n 1.0**, qui laissaient croire à une protection inexistante. Sauvegarde de 164 Mo prise avant recreate ; après : éditeur 200, **32 workflows actifs et 22 webhooks retrouvés**, plus aucune dépréciation au boot hors les deux lourdes (C1c). | Dev | 2026-09-23 |
| C14 | **Fenêtre de maintenance déclarée.** La veille ne distinguait pas un déploiement d'une panne — un build Next d'Audityo à 335 % de CPU ressemble à un processus emballé. Commande `maintenance start [minutes] | stop | status` sur le VPS, et une garde dans `alert()` du watchdog. Trois exigences tenues : **bornée par construction** (4 h au maximum, jamais de fenêtre qu'on oublie de refermer), **jamais silencieuse** (chaque alerte supprimée est journalisée avec le temps restant), et **le signe de vie du lundi n'est pas concerné** — il appelle `notify()` en direct, un moniteur mort doit rester distinguable d'un moniteur calme. Vérifiée sur copie isolée : hystérésis comptée normalement aux passages 1 et 2, puis « alerte supprimee, fenetre de maintenance (fin dans 223 min) ». | Dev | 2026-09-24 |
| C15b | **Les trois sondes sont décalées de la minute ronde** : veille à `7-59/15`, disponibilité à `1-59/5`, Audityo à `3-59/5`. Elles ne mesurent plus le moment où le CPU de l'hôte est le plus disputé, et ne se marchent plus dessus. Vérifié sur 18 heures : exécutions à :07/:22/:37/:52, :41/:46 et :43/:48. | Dev | 2026-09-24 |
| C19 | **Double comptage GA4 coupe.** « Changements de pages selon les evenements de l'historique du navigateur » desactive sur le flux `G-FMCN24CNYW` (propriete 527616733), verifie en rouvrant le panneau apres enregistrement. Ouvert depuis le 13/08/2026. « Chargements de pages », grise et non desactivable, est sans effet chez nous : `send_page_view: false` et `page_view` emis a la main par `Analytics.tsx`. **Trouvaille en passant, et elle compte plus que le reglage** : la propriete affiche « collecte non active », mais le tag n'est pas casse — il est bien dans le bundle de production, `VITE_ENABLE_ANALYTICS` est a `true`, et le rapport donne **1 utilisateur et 13 evenements sur 7 jours**. L'avertissement de Google est un seuil de volume, pas un diagnostic. On vient de fiabiliser la mesure d'une audience qui tient dans une main. | Marketing | 2026-09-24 |
| C3 | **Cle fuitee supprimee le 25/09.** « Chat distr'action 2026 » (`2da...642`) etait lisible dans `n8n-workflows/README.md`, depot public, **depuis le 5 avril** — cinq mois et demi. Supprimee dans la console OpenRouter sur accord de Laurent, puis **prouve morte** : appel a `/api/v1/key` avec la valeur reelle → **401**. *(Premiere tentative de preuve invalide : le fichier temporaire n'avait pas ete trouve, l'en-tete partait vide et le 401 ne prouvait rien. Refait en un seul processus, sans passer par le shell.)* Les trois occurrences retirees du fichier et remplacees par `<VOTRE_CLE_OPENROUTER>`, avec renvoi a 1Password. **La valeur reste dans l'historique git et c'est sans consequence** : la cle n'existe plus. Aucun conteneur ne la portait, derniere utilisation il y a huit mois, 9,50 $ a vie — aucun abus. | Dev | 2026-09-25 |
| C23 | **Chantier fantome — ferme le 25/09 sans rien changer.** La note disait que « Notre Histoire » racontait 2019-2026 contre un `foundingDate: "2025"` dans le JSON-LD. **La frise avait deja ete corrigee le 14/09**, deux jours avant que la note ne soit versee au carnet : un commentaire de `AboutPage.tsx` documente le retrait des jalons 2019, 2022 et 2023, « qui n'ont pas eu lieu ». Verifie dans le code *et* en production : `history` ne porte plus que **2025 et 2026**, en accord avec `foundingDate`. *Deuxieme fantome de la semaine apres C29 — une note ecrite a partir d'une autre note, sans retour a la source.* | Marketing | 2026-09-25 |
| C24 | **Accord obtenu — Laurent le confirme le 25/09.** `TL Services` etait nomme en production depuis le 04/09 par le bloc SEO, qui imprime la cle `client` de chaque fiche sans filtre. L'accord regularise ce qui etait deja en ligne, ce qui etait tout l'enjeu. **Rien a modifier** : le nom peut rester. *Reste a ranger la trace ecrite de cet accord quelque part de retrouvable — le carnet enregistre la parole de Laurent, il ne remplace pas le document.* Voir [ADR-006](decisions/ADR-006-nommer-nostalgie.md). | Légal | 2026-09-25 |
| C42 | **L'outil de scraping est sorti de l'Internet public — 25/09.** En cherchant *pourquoi* il etait ouvert, j'ai regarde ce qu'il expose : **nom, ville, telephone, e-mail et adresse** de professionnels belges et francais — medecins, avocats, plombiers, coiffeurs — avec un bouton **« Telecharger CSV »** et **aucune authentification**. Ce sont des **donnees personnelles** au sens du RGPD : un plombier independant est une personne physique. N'importe qui sur Internet pouvait extraire le fichier. **Deux portes fermees** : le port 5000 passe de `0.0.0.0` a `127.0.0.1`, et `traefik.enable` passe a `false`. Verifie : 5000 ferme depuis Internet, `scraping.srv767464.hstgr.cloud` rend **404**, et l'application repond toujours **200 en local sur le VPS**. Acces retabli par tunnel : `ssh -L 5000:127.0.0.1:5000 root@193.203.191.251` puis `http://localhost:5000`. **Reversible en inversant deux lignes** du compose, le jour ou une authentification sera posee. | Dev | 2026-09-25 |
| C38 | **Journal Traefik rendu silencieux — 25/09.** Trois causes distinctes, trouvees en lisant les journaux **avant** de poser C37 (savoir ce qui criait deja). (1) `cron-dreamoracle` : le service n'avait **aucun label Traefik** — la vraie cause est que Traefik tourne avec `--providers.docker` **sans `exposedByDefault=false`**, donc il ramasse tous les conteneurs, y compris un cron qui lance des `curl` et n'a pas de port. `traefik.enable=false` pose. (2) `Router scraping@docker uses a nonexistent resolver: myresolver` : le seul resolveur declare est `letsencrypt`. **Le certificat existait pourtant et se renouvelait** — verifie dans `acme.json`, il est bien range sous `letsencrypt`. L'erreur etait du bruit, pas une panne ; nom corrige, aucune demande nouvelle declenchee. (3) ACME en echec pour `audityo.fr`, `.org` et `.online` : **aucun des trois ne resout vers une IP publique**, l'echec etait donc perpetuel. 30 lignes de labels **commentees** plutot que supprimees, avec la consigne de decommenter le jour ou le DNS pointera. Apres : les quatre sites repondent 200 avec TLS valide, et **zero erreur** dans le journal. | Dev | 2026-09-25 |
| C37 | **Limitation de debit posee sur les trois webhooks de chatbot — 25/09.** Middleware Traefik `chatbot-ratelimit` : **20 requetes par minute et par IP, rafale de 10**, sur un routeur dedie (`n8n-chat`, priorite 100) qui ne vise que `/webhook/ainspiration`, `/webhook/chatbot` et `/webhook/audityo-chat`. Le reste de n8n n'est pas concerne. Comptage juste : Traefik est en frontal direct sur 80/443, sans proxy devant, il voit la vraie adresse du client. Un echange coute deux requetes (preflight CORS + message), soit ~10 messages par minute et par IP : large pour un humain, etroit pour une boucle. **Eprouve** : 35 appels rapides → 10 passent puis 429 ; les trois chatbots repondent normalement depuis une autre IP. Test fait en **GET** pour que Traefik tranche avant n8n — aucune requete n'a coute un appel au modele. Compose sauvegarde avant modification, `docker compose config` valide, Traefik n'emet aucune plainte sur les nouvelles regles. | Dev | 2026-09-25 |
| C36 | **Chatbot Audityo construit et mis en service le 25/09.** Workflow `Audityo — Chat (EU AI Act)` (`rdqUmTQThti94POA`), 9 noeuds, valide par n8n sans erreur ni avertissement, actif. Meme methode que les deux autres : le blog est lu a chaque message sur `/api/blog` (18 articles) et les plus proches de la question sont injectes avec leur URL. Credential dediee `OpenRouter-Audityo-Prod` (1 $/jour, `daily` verifie) — le cloisonnement du matin tient, quatre projets, quatre cles. **Garde-fous propres au sujet reglementaire** : aucun conseil juridique, **aucune classification du systeme d'un visiteur** (un niveau suppose un inventaire, c'est le produit), Audityo **n'est pas ouvert** donc la grille 49/199/499 € est annoncee « prevue a l'ouverture » avec « rien n'est facture a ce jour » et **aucune date promise**. Tous les articles, seuils et dates releves sur la page d'accueil le 25/09. Eprouve sur six questions dont trois pieges. **Reste le widget**, qui appartient a une session Audityo — contrat d'API dans [`docs/chantiers/audityo-chat-integration.md`](../docs/chantiers/audityo-chat-integration.md). | Marketing | 2026-09-25 |
| C35 | **Le chatbot lit le site à chaque message — fait le 25/09.** Il ne recopie rien : deux noeuds HTTP interrogent `/seo-routes.json` (34 pages, titre + description) et `/api/blog-posts` (33 articles), puis le noeud `Code` assemble un contexte de ~4,5 Ko en **53 ms**. Les pages sont donnees en entier — c'est le catalogue ; les articles sont **filtres par proximite** avec la question (recouvrement de mots, repli sur les trois plus recents si rien ne correspond). **Aucune synchronisation à tenir** : le site est sa propre source. Les deux noeuds sont en `continueRegularOutput` et le `Code` en try/catch — une source injoignable degrade la reponse sans l'empecher, ce qui a ete verifie en vrai (le premier essai, qui utilisait `fetch` dans le noeud Code, echouait faute de reseau dans le sandbox : le chatbot a repondu normalement, sans rien inventer). Verifie sur la version publiee. **Transpose au chatbot Distr Action le meme jour**, mais pas a l identique : ce site n a pas de `seo-routes.json` et n en a pas besoin, son catalogue de services vient deja d un Google Sheet ; seul le blog (12 articles) manquait. **Rythme commercial corrige sur retour de Laurent** : jamais de montant dans la premiere reponse, mais reponse franche des qu on demande un prix. **Trouvaille en chemin** : `www.distr-action.com` repond **200 a toute URL inconnue** en servant la page d accueil — le defaut qu AInspiration a corrige le 13/08 — ce qui prive en plus le garde anti-invention de son filet (un lien fabrique y repondrait 200). Consigne dans [`docs/chantiers/distraction-chat-et-spa-fallback.md`](../docs/chantiers/distraction-chat-et-spa-fallback.md), a executer depuis une session Distr Action. | Marketing | 2026-09-25 |
| C33 | **Pipeline d'audit realigne le 25/09 — la machine reste, la promesse part.** Arbitrage pris apres avoir regarde ce que le workflow fait vraiment : l'e-mail est envoye **avant** la reponse HTTP, donc le rapport est deja dans la boite quand le prospect lit « sous 24h ». La promesse etait fausse **dans le mauvais sens**. Le pre-diagnostic, lui, est reel, automatique, immediat et alimente le CRM : c'est un vrai aimant a prospects, on le garde. Retires : le nom de l'offre abandonnee (« Votre Audit IA Personnalise » → « Votre pre-diagnostic IA », objet compris), le delai invente, « call gratuit », et l'adresse **Enghien** (fausse : Givry). Ajoutes : une phrase disant que le resume est **genere automatiquement a partir des reponses** et n'est pas un examen de processus, et « (estimation) » sur les gains de temps que le modele projette. Le bouton pointe desormais **cal.com/ainspiration/30min** au lieu de `/contact`. Verifie sur la version **publiee** : zero motif interdit restant. | Marketing | 2026-09-25 |
| C34 | **TVA corrigee dans les trois langues le 25/09.** `BE 0795.938.932` retire sur decision de Laurent, remplace par **BE 0462.122.648** dans `public/locales/{fr,en,nl}/legal.json`. JSON revalides. Deploye selon l'ordre obligatoire : build, **manifeste pousse avant Netlify**, deploiement, verification des entrees une a une sur le CDN, puis recreate du conteneur. | Légal | 2026-09-25 |
| C31 | **Credential OpenRouter de n8n scindee en trois — 25/09.** `OpenRouter-Radio-Prod`, `OpenRouter-AInspiration-Prod` et `OpenRouter-DistrAction-Prod`, 1 $/jour chacune et `daily` verifie ; **12 noeuds / 9 workflows** repointes. Controle fait sur les **versions publiees**, pas sur les brouillons : zero reference restante a `OpenRouter account`, ni en brouillon ni en publie. Le repointage s'auto-publie (verifie sur `chat distr'action V4`). Preuve de bout en bout : le chatbot AInspiration repond en 4 s avec la nouvelle cle. *La premiere lecture de la base disait « aucune credential creee » — je ne copiais pas le journal WAL de SQLite. Toutes mes lectures du jour avaient ce defaut.* | Dev | 2026-09-25 |
| C32 | **Le chatbot du site inventait tout, purge le 25/09.** Decouvert en testant C31. Son prompt systeme ne portait pas une trace de l'offre abandonnee, il en etait fait — et bien pire : **trois dirigeants qui n'existent pas**, **trois etudes de cas inventees** avec des montants (45 k€, 65 k€), des chiffres fabriques (**ROI 180 %, 50+ PME accompagnees, 98 % satisfaction, precision 99,9 %**), la **grille tarifaire retiree le 18/09** (49 €/99 € par mois) avec un « SLA garanti » que l'ADR-005 interdit, l'**audit gratuit en 24 h sans engagement**, une **adresse fausse** et la mention « SPRL ». Quatre decisions violees d'un coup, en production, sur le seul tunnel de conversion. Prompt reecrit : uniquement des faits verifiables, la grille O1–O5, le rendez-vous de decouverte, et une **regle absolue placee avant toutes les autres** interdisant d'inventer chiffre, client, collaborateur, delai ou garantie. Bouton « Reserver mon audit gratuit » remplace par le lien cal.com reel, avec un garde de derniere ligne dans le noeud `completion` au cas ou le modele rendrait l'ancienne valeur. Verifie sur trois questions pieges. | Marketing | 2026-09-25 |
| C30 | **Repare le 25/09 — et le defaut etait pire que decrit.** Je croyais que `communityos` ne redemarrerait pas ; en realite un `docker compose up` lance dans son repertoire **monte une pile parallele**. Compose n'y trouve aucun fichier a son nom par defaut, remonte d'un cran, charge `/docker/communityos/src/docker-compose.yml` (une reference de developpement) et cree un projet **`src`** a cote de `communityos`. Rien n'avertit. **Correctif structurel plutot que rustine** : `/usr/local/bin/compose-exact <conteneur> …` relit les labels `com.docker.compose.*` — projet, fichiers, **et** fichier d'environnement, que Docker enregistre tous — et reconstruit l'invocation exacte. La source de verite devient le conteneur qui tourne, pas la memoire de celui qui tape. Guerit toute la classe : `audityo` (deux fichiers), `audityo-dev` (`docker-compose.dev.yml`), `communityos`. Avertissement pose dans le repertoire piege. `communityos-web` recree sur sa propre cle, *healthy*, aucun projet `src` cree. Miroir : `docs/ops/vps/compose-exact`. | Dev | 2026-09-25 |
| C29 | **Fausse alerte, levee le 25/09 — et c'est moi qui l'avais fabriquee.** J'avais conclu que `audityo-dev` ne redemarrerait pas, son compose declarant un `env_file: .env` inexistant. J'avais lu `/root/audityo-dev/docker-compose.yml` — le fichier au **nom par defaut**, celui que Compose choisit tout seul, et **qui ne sert pas ici**. Le fichier actif est `docker-compose.dev.yml`, qui declare `env_file: .env.dev` : ce fichier existe, en 600. `compose-exact audityo-dev-web config` valide. *Le piege de C30 m'avait attrape avant que je le nomme.* La pile est saine ; reste a y coller la cle `audityo-dev`. | Dev | 2026-09-25 |
| C4 | **Collecte POP3 de Gmail retirée par Laurent le 24/09.** Ce n'était pas une panne à réparer : il ne voulait plus lire `info@` dans Gmail, et la boîte est déjà relevée trois fois par jour par la tâche `releve-emails` (C26) — la collecte faisait doublon. ⚠️ **Le journal d'accès ne le confirme pas encore, et c'est normal** : dernière tentative le 23/09 à 23h28, mais l'écart précédent était déjà de 21 h 16. Le silence ne vaudra preuve qu'après le 25/09. *(Conservé parce que la méthode resservira : la temporisation exponentielle de Gmail avant désactivation se lit dans les journaux d'accès Hostinger — 1 h 41, 4 h 09, 9 h 31, 21 h 16 — elle ne se devine pas.)* | Opérations | 2026-09-24 |
| C26 | *(Ouvert et refermé le 24/09 — il n'y avait pas de trou.)* Je craignais qu'en retirant la collecte Gmail, `info@ainspiration.eu` ne soit plus lu nulle part. **La tâche planifiée `releve-emails` le relève déjà trois fois par jour** (8h/13h/18h), par le même connecteur Hostinger, aux côtés des quatre autres boîtes. La collecte Gmail faisait doublon — c'est ce qui la rendait inutile. Seule limite, connue et assumée : la tâche ne tourne que si l'application est ouverte. | Opérations | 2026-09-24 |
| C8 | **Vérifié le 24/09, et la parution n'était pas ce jour-là.** Le workflow tire un jour pseudo-aléatoire déterministe par semaine ; pour la semaine 39 c'est **vendredi 25/09**. Le « jeudi 24/09 » du HANDOFF était une supposition (« 16/09 + 8 jours »), pas un calcul — l'auto-blog a bien tourné à 05h30 et s'est arrêté en 0,43 s, décision normale. **Reste à constater vendredi que les trois langues sortent** : c'est le premier vrai test du correctif antislashes du 16/09. | Dev | 2026-09-24 |
| C27 | **Le générateur allait réécrire l'offre abandonnée.** Son prompt listait `audit gratuit 24h, mise en place type 48h` parmi les « faits vérifiables » autorisés : les 35 articles purgés la veille se seraient reremplis, une semaine sur deux. Purger un corpus sans fermer la source ne sert à rien. Prompt réécrit sur le rendez-vous de découverte et le Diagnostic à 2 400 €, et deux motifs ajoutés au contrôle qualité comme garde de dernière ligne — testés sur 16 cas : ils attrapent « audit gratuit », « analyse gratuite », « mis en place en 48h », et laissent passer « outils IA gratuits », « version gratuite de SEMrush », « chatbot 24h/24 ». | Marketing | 2026-09-24 |
| C28 | **Le pool de réalisations de l'auto-blog avait dérivé du dépôt**, malgré la mention « KEEP IN SYNC » : `labo-nostalgie` n'existait dans aucune fiche (panne muette en attente le jour où ce slug serait sorti), `playlists-auditeurs` manquait alors que sa fiche est complète, et `paperclip` y figurait malgré son `status: 'interne'` — **c'était le tirage du 25/09**. On allait publier un retour d'expérience sur un outil interne arrêté le 17/09. Liste réalignée sur `realisations.ts`, dans le même ordre pour que la synchro se vérifie à l'œil. Le tirage de vendredi devient `autoseo`. | Dev | 2026-09-24 |
| C16 | **Paquets x2go purges.** 12 paquets retires, plus l utilisateur systeme x2goprint et son crontab. La decision s est prise sur l utilite du service — aucune session ouverte depuis l image d origine du 22/05/2025 — et non sur une moyenne sar que le bruit des builds Audityo aurait toujours rendue indecise. Verifie apres coup : SSH actif, 44 conteneurs debout, site en 200 a 92 ms. | Dev | 2026-09-24 |
| C1d | **Migration du stockage n8n faite.** N8N_MIGRATE_FS_STORAGE_PATH pose, conteneur recree : binaryData a disparu, storage existe. Le bind-mount portait sur tout le repertoire .n8n et non sur binaryData, donc **aucune modification de montage n a ete necessaire** — contrairement a ce que l avertissement laissait craindre. Sauvegarde de 164 Mo prise avant. Apres : editeur 200, 32 workflows actifs, 22 webhooks. Le message de depreciation est passe au pluriel au singulier : cinq sur six traitees, reste le runner (C1c). | Dev | 2026-09-24 |
| C18 | Le lien de réservation Cal.com ouvre les boutons de rendez-vous, et le libellé annonce enfin trente minutes | Ventes | 2026-09-22 |
| C1 | Image n8n épinglée sur `2.40.5` dans `/root/docker-compose.yml`, image validée taguée localement pour qu'un recreate n'aille rien chercher sur le réseau. Compose sauvegardé en `.bak-20260923`, n8n non redémarré. *(Reste les dépréciations → C1b.)* | Dev | 2026-09-23 |
| C15 | Correctif du watchdog du 21/09 vérifié : **plus aucune ligne « steal au-dessus du seuil »** en deux jours, les dernières datent du 21/09 à 7h30 et annonçaient le retour à la normale. *(Reste le décalage des cron → C15b.)* | Dev | 2026-09-23 |
| C9 | **Versions EN et NL de l'article du 16/09 publiées**, et le contrôle de santé est passé à **0 échec, 0 avertissement, 27 vérifications** — il était rouge chaque lundi depuis le 19/09. Il a d'abord fallu corriger l'article français, qui vendait l'offre abandonnée : le traduire l'aurait propagée en deux langues. Les `hreflang` se construisent par convention de slug (`base`, `base-en`, `base-nl`, publiés), sans colonne `translation_of` — quatre liens alternatifs vérifiés en production. Liens internes pointés vers `/en/…` et `/nl/…` plutôt que vers les URL françaises, pour que le lecteur reste dans sa langue. | Dev | 2026-09-23 |
| C25 | **L'offre abandonnée purgée du blog et de la vitrine.** 35 articles corrigés, 43 remplacements, sauvegarde `blog_posts_backup_20260923`. Et la description de `/audit` servie en production, qui promettait encore « plan d'action concret en 24h. Gratuit, sans engagement » — sur la page qui porte le seul tunnel de conversion, dans ce que Google affiche. La purge du 18/09 portait sur les pages : ni le blog ni `seoConfig.ts` n'avaient été relus, et son « zéro occurrence » était donc faux. Quatre passes par motifs ont échoué l'une après l'autre ; ce qui a conclu, c'est de lire **tous** les appels à l'action (25) et **toutes** les conclusions (164) sans filtre de mots. | Marketing | 2026-09-23 |
| C5 | Fiche et société de test supprimées du CRM. Les identifiants notés au HANDOFF étaient faux — les vrais étaient `15b76060…` et `d3aab942…`. Dépendances vérifiées avant suppression (0 opportunité, 0 tâche, 0 message, un seul contact sur la société), puis suppression en transaction : 2 activités, 1 contact, 1 société. Contrôle après coup : rien ne reste, aucune activité orpheline. | Opérations | 2026-09-23 |
| C22 | **Aucun jeton ni clé en clair dans l'instance n8n.** Contrôle porté aux **41 workflows**, pas seulement aux deux visés : zéro JWT littéral (`eyJ`), zéro clé `sk-`. Tous les `Bearer ` sont suivis d'une expression. La crainte du 16/09 est levée pour toute l'instance. Méthode : comptage d'occurrences en lecture seule sur la base SQLite, **sans jamais afficher de valeur**. | Dev | 2026-09-23 |
