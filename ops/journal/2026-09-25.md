

## `distraction2026` : une seconde cle maitresse, invisible a la methode employee

Elle n'etait dans l'environnement d'aucun conteneur pour une raison simple : **elle n'y est
pas**. C'est la credential n8n `gtjfsOGk7WrWy7p3` (« OpenRouter account »), stockee **chiffree
dans la base de n8n**. Une cartographie par `printenv` ne pouvait structurellement pas la voir.

*La methode qui a servi a cloisonner sept conteneurs avait un angle mort, et l'angle mort
contenait exactement ce qu'on cherchait.*

**Comment elle a ete identifiee**, sans jamais faire sortir la valeur :

1. La base SQLite de n8n, copiee en lecture seule, donne les **noms et types** des
   credentials : une seule de type `openRouterApi`.
2. Un balayage des `nodes` de tous les workflows dit **qui la reference**.
3. `n8n export:credentials --decrypted`, execute **dans le conteneur**, dont on ne fait
   ressortir que le **masque** `49e...4a6` — exactement ce que la console OpenRouter affiche
   d'elle-meme. Correspondance avec `distraction2026` etablie.

**Huit workflows actifs, quatre projets** se la partagent :

| Projet | Workflows |
|---|---|
| AInspiration | `chat Ainspiration - TEXT ONLY`, `Audit IA Pipeline` |
| Distr'Action | `chat distr'action V4` |
| Radio | `120 min 2026` (2 noeuds), `Generation Playlist — CLASSIQUE` (2 noeuds), `Generateur playlist Spotify` |

Plus trois workflows inactifs (`Newsletter Automation`, `Playlist ETE`, `AUDIT Langue FR-INT`).

Etat : plafond **2 $/jour**, deja journalier ; 40,15 $ cumules, **0,98 $ cette semaine**. Elle
sert tous les jours.

**Consequence : n8n consomme deux cles.** `n8n-prod` par `$env` — les auto-blogs et leurs
traductions — et `distraction2026` par credential. Le cloisonnement d'aujourd'hui n'a traite
que la premiere. Ouvert en **C31** : trois credentials distinctes et les noeuds repointes.

Rien n'est urgent : elle est plafonnee, journaliere, et sa depense est modeste. Mais elle
rouvre exactement le probleme qu'on vient de fermer — une depense qu'on ne sait pas rattacher
a un projet, et une rotation impossible sans tout casser d'un coup.
