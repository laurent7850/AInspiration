# 2026-09-25

## L'auto-blog publie en trois langues — le correctif antislashes tient

C'etait le rendez-vous pris hier : premier vrai test du correctif du 16/09, depuis que
`Parser FR` retire les antislashes que le modele glisse dans son HTML.

`automatisation-seo-trois-marques-distr-action-2026-09-25`, publie a **05h53-05h54** dans les
trois langues. Le sujet est celui qu'on attendait (`autoseo`), preuve que le realignement du
pool de realisations de C28 a bien pris — la veille, le tirage designait encore `paperclip`,
un outil interne arrete le 17/09.

Verifie sur le **HTML servi sans JavaScript**, seul test qui compte pour cette classe de
defaut :

| | FR | EN | NL |
|---|---|---|---|
| Code | 200 | 200 | 200 |
| `<h2>` dans le HTML brut | 8 | 8 | 8 |
| Antislashes parasites | 0 | 0 | 0 |
| Liens `hreflang` | 4 | 4 | 4 |

Les quatre `hreflang` repondent **200 en direct**, sans redirection, et le `canonical` designe
bien la version francaise. Les URL prefixees `/en/blog/<slug>-en` renvoient un 301 vers
`/blog/<slug>-en` : c'est la convention en place, la langue vit dans le slug.

Aucune trace de l'offre abandonnee dans les trois corps — les deux motifs ajoutes au controle
qualite (C27) n'ont pas eu a servir, le prompt corrige a suffi.

**C8 est reellement clos.** Un chantier qui attend une preuve n'est clos que le jour ou la
preuve arrive.

## `brasspat042026` cloisonne — Maudios ne porte plus que deux conteneurs

Troisieme tentative, la bonne. Les deux premieres avaient reecrit **la meme cle** : la fiche
1Password n'avait pas suivi la recreation. Cette fois le script a annonce
`cle vivante, plafond journalier.` avant d'ecrire — le garde-fou ajoute hier soir fait son
travail, la preuve est produite au moment du geste et non apres coup.

Conteneur recree, *healthy*, empreinte conforme entre le `.env` et l'environnement.

`Maudios` est passee de **sept conteneurs a deux** : `audityo-dev-web` et `communityos-web`,
les deux seuls dont le compose est casse (C29 et C30). *Le cloisonnement bute exactement la
ou l'infrastructure etait deja fragile — ce n'est pas une coincidence, c'est le meme
symptome.*

On ne revoque toujours rien : la regle des 24 a 48 h de trafic nul sur `Maudios` ne peut
commencer a courir qu'une fois ces deux-la passes.
