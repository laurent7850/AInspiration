

**Termine dans la foulee.** Les sept cles sont passees en `Daily`, verifie par l'API sur les
cinq deployees et par le badge `TODAY` sur les sept. Le reglage s'edite sur une cle existante,
sans rien recreer — et le ✓ vert est le piege : sans lui, le champ n'est pas enregistre.

Un mot sur la methode, parce qu'elle a failli couter cher : nous avons pilote le meme
navigateur a deux, et les clics se sont marches dessus pendant plusieurs minutes. Le seul
etat fiable pendant ce temps n'etait pas l'ecran, c'etait l'API. *Quand la mesure est
disputee, prendre celle qui ne depend pas de qui tient la souris.*

Reste une seule chose, et elle n'est pas pour Claude : `/docker/brasspat042026/.env` porte
encore la cle **morte** (401). La nouvelle `brasspat-prod` existe et est plafonnee, mais sa
valeur doit etre reportee par Laurent :

```
ssh -t -i ~/.ssh/vps767464_ed25519 root@193.203.191.251 "/root/poser-cles-openrouter.sh brasspat-prod"
```

Puis `docker compose up -d --force-recreate app` dans `/docker/brasspat042026`.
