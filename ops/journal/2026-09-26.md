# 26 septembre 2026

notion: non

## L'alerte « dockerd emballé » : c'était mon propre processus oublié

Laurent reçoit depuis le 25/09 une alerte toutes les six heures — `dockerd` à 104 % d'un
cœur. Vérifié ce matin : **l'alerte était juste**, et au moment où je l'ai mesurée le
démon était même monté à **193 %**, soit deux cœurs sur quatre. 21 h 44 de CPU cumulé.

### Ce que le démon faisait vraiment

Le mail du watchdog suggérait la piste du 17/09 — désynchronisation avec containerd,
redémarrage du démon. **Ce n'était pas ça.** Le journal `docker` était vide sur deux
heures, donc rien à lire de ce côté. C'est `strace -c` qui a parlé :

```
 5.60    2.633092          24    109075           pread64
```

**109 075 lectures en 8 secondes, soit ~13 600 par seconde.** Une boucle. Le descripteur
visé, résolu par `/proc/<pid>/fd` :

```
fd 17 -> /var/lib/docker/containers/1e89abd3…-json.log (deleted)
```

Un fichier de log **supprimé**, relu 7 000 fois par seconde. Et en face, un client :

```
352154  17:13:37  docker logs scraping-belgique-web-1 --tail 3
```

### C'est moi qui l'ai laissé là

Le parent de ce processus est une de **mes** commandes de diagnostic du 25/09, celle de
l'inventaire C39/C41/C42 : elle inspectait `dreamoracle-cron-1`, puis
`scraping-belgique-web-1`, curlait son URL HTTPS, et finissait par
`docker logs scraping-belgique-web-1 --tail 3`. Lancée à **11:02:38**.

La corrélation ne laisse aucune place au doute :

| | |
|---|---|
| Ma commande démarre | 25/09 **11:02:38** |
| Premier dépassement de seuil | 25/09 **11:07:49** |
| Alerte confirmée et envoyée | 25/09 **11:37:48** |

Cinq minutes. Le watchdog a fait exactement son travail ; c'est la cause qui était à moi.

### Ce que je peux établir, et ce que je ne peux pas

**Établi.** Un client `docker logs` resté accroché fait tourner le suiveur de log de
`dockerd` en boucle. Le tuer suffit : le démon retombe immédiatement, sans redémarrage.

**Établi aussi, et il faut corriger l'ordre des choses.** J'ai d'abord cru que la
suppression du fichier était la cause. Elle ne l'est pas. Le conteneur n'a été recréé
qu'à **14:00:39** — près de trois heures *après* le début de la flambée — quand j'ai
appliqué le correctif C42. La suppression du log n'a donc fait qu'ajouter le `(deleted)`
qui a rendu le symptôme lisible ; la boucle tournait déjà.

**Non établi : pourquoi `docker logs --tail 3` s'est bloqué.** Rejoué ce matin sur le même
conteneur, il rend la main en 0 s. Le conteneur n'a ni `tty` ni `stdin_open`. La rotation
est armée (`max-size: 10m`, `max-file: 3`) et un suiveur présent pendant une rotation est
un déclencheur connu de cette classe de boucle chez Docker — mais je n'en ai pas la preuve
ici, et je ne vais pas l'écrire comme si je l'avais.

### Corrigé, et vérifié

`kill 352154` (plus son shell parent 352120). Mesuré avant / après :

| | avant | après |
|---|---|---|
| `dockerd` | 104 → 193 % | **1,7 – 16,6 %** |
| `pread64` | 109 075 / 8 s | **7 / 5 s** |
| Descripteurs supprimés ouverts | 1 | **0** |
| Charge (1 min) | 1,85 | **0,37** |

Les quatre sites répondent (200, 200, 301, 307 — les deux derniers sont leurs redirections
normales). Le watchdog a confirmé de lui-même à **04:52** : *« runaway revenu à la
normale »*, et a envoyé le mail de retour à la normale. **Aucun redémarrage de démon.**

### Le garde-fou : que la prochaine alerte se passe de moi

Le défaut n'était pas la panne, c'était que l'alerte ne disait pas *pourquoi*. Elle
nommait `dockerd` et renvoyait vers une cause du 17/09 qui n'était pas la bonne — de quoi
chercher longtemps.

`/opt/vps-watchdog.sh` gagne une fonction `contexte_dockerd`, appelée **seulement** quand
le processus emballé est `dockerd`. Elle ajoute au mail : le nombre de descripteurs de log
supprimés encore ouverts, et la liste des clients `docker logs`/`attach`/`events`/`stats`/
`wait` accrochés depuis plus de cinq minutes, avec leur âge et leur PID. Plus la consigne :
tuer le client, **ne pas** redémarrer le démon.

Quand il n'y a aucun client accroché, elle le dit et renvoie alors vers la piste
containerd — celle du 17/09 redevient pertinente précisément dans ce cas.

**Deux pièges rencontrés en l'écrivant, tous deux déjà dans mes notes :**

- *Le heredoc mange les doubles antislashes.* Mon premier script de patch est arrivé sur
  le VPS avec `'…suffi.\n\n…'` où `\n` était devenu une **vraie** fin de ligne Python au
  lieu d'un antislash-n littéral — d'où un `assert` qui a sauvé la mise, le fichier
  n'étant écrit qu'en fin de script. Réécrit sans **aucun** antislash littéral :
  `B = chr(92)` et concaténation. C'est la seule forme qui traverse shell Windows + ssh
  sans se faire abîmer.
- *Un filtre qui se dénonce lui-même.* Le premier `grep -E 'docker (logs|…)'` attrapait
  aussi le shell qui *mentionne* la commande — mon propre diagnostic apparaissait dans sa
  propre sortie. Resserré sur `ps -o comm`, qui vaut `docker` pour le client et `bash`
  pour son shell. Les deux branches retestées, dont celle « client présent » en fabriquant
  la condition avec un vrai `docker logs -f`, puis en la retirant.

### La leçon, pour moi

**Toute commande de diagnostic qui appelle `docker logs` se borne par `timeout`.** Sans
borne, un client qui ne rend pas la main coûte un à deux cœurs pendant des heures, et
l'alerte qui en résulte fait chercher une panne qui n'existe pas. Ouvert et clos en C45.

---

## C10 : l'offre abandonnée était encore publiée à Google

Le chantier C10 demandait d'arbitrer « Mise en place en 48h » et « Données 100% sécurisées ».
En le prenant, j'ai trouvé pire, et ailleurs.

### Un lecteur voyait une chose, les moteurs en recevaient une autre

La FAQ visible de l'accueil est propre dans les trois langues depuis le 16/09 : la question
sur l'audit gratuit a été remplacée par « Le rendez-vous de découverte engage-t-il à quelque
chose ? — Non. Trente minutes pour comprendre votre situation. »

Mais `HomePage.tsx` portait un **second jeu de questions en dur**, en français seulement,
donné à `getFAQSchema()` — donc publié en données structurées JSON-LD :

> « L'audit est-il vraiment gratuit et sans engagement ? — Oui, 100% gratuit et sans
> engagement. Nous analysons votre activité et vous livrons un plan d'action concret. »

C'est **l'offre abandonnée le 16/09**, celle dont le prompt du chatbot avait été purgé la
veille. Le même texte vivait une troisième fois dans le repli i18n de `FAQ.tsx`.

**La duplication était la cause, pas le texte.** Les locales avaient été corrigées ; les deux
copies en dur n'avaient pas suivi, et rien ne pouvait le signaler. J'ai donc posé une source
unique — `src/data/faq.ts`, **générée depuis `public/locales/fr/common.json`** et importée par
les deux appelants. Elles ne peuvent plus diverger sans qu'on le veuille.

Même défaut sur `/transformation` : sa FAQ visible *et* son `getServiceSchema` promettaient
« premiers résultats en 5 jours ». La troisième réponse de cette même FAQ avait pourtant déjà
été réécrite honnêtement — celle-là avait été oubliée.

### Un chiffre qui contredisait son propre libellé

`AnimatedStats` — sur le **Hero de l'accueil** et sur `/solutions` — affichait **« 24h »**
sous le libellé « Pour en parler trente minutes ». Le libellé avait été réécrit, le chiffre
jamais. C'est le genre de défaut qu'aucune relecture de locale ne trouve : le texte est dans
un JSON, le nombre est dans le TSX.

Le reste, corrigé :

| Où | Avant | Après |
|---|---|---|
| `AnimatedStats` + `Consulting` | `48h` « Mise en place type » | **14** réalisations en service |
| `video.json` ×3 | `48h` / Première vidéo | `30 min` / Pour en parler |
| `video.json` ×3 | `-80%` / Coûts vs tournage | même chiffre, **cadré en objectif** |
| `seoConfig.ts` **et** `routes/seo.js` | « premier résultat concret en 5 jours » | « du périmètre au déploiement » |

Le « 5 jours » vivait **en double, front et backend** : la meta de `/solutions` est écrite aux
deux endroits, et seul le backend la sert dans le HTML brut. Corriger un seul des deux aurait
laissé le défaut à moitié.

Les `100%` restants sont défendables : ils portent tous le libellé « Données hébergées en
Europe », ce qui est vrai.

### Ce que je n'ai pas tranché

Deux promesses de **délai de réponse** — « Réponse sous 24h » sur `/pme-hainaut-bruxelles`
(trois langues) et « Je réponds sous 24 heures ouvrées » dans `forms.json` (FR seulement).
Ce ne sont pas des chiffres inventés comme les précédents, c'est un engagement personnel
tenable. À garder s'il le tient, à retirer sinon : c'est son arbitrage, pas le mien. C10 reste
ouvert là-dessus, et sur rien d'autre.

### Mon premier balayage était trop étroit

J'avais grepé `public/locales`, `src/config`, `src/data` et `docker/backend`. Tout ce qui est
raconté ci-dessus vit dans `src/pages` et `src/components` — je l'ai manqué au premier passage
et je n'ai trouvé qu'en balayant `src/` en entier. **Pour chercher une promesse dans ce dépôt,
il faut balayer tout `src/`, pas les dossiers où l'on suppose que le contenu vit** : la moitié
des chiffres sont en dur dans les composants, et les JSON-LD sont écrits dans les pages.

Trouvé au même passage : `auditLiveProtocol.ts`, `auditReportTemplate.ts` et
`postAuditEmails.ts` portent encore l'offre retirée, et **aucun import ne les cite** — ils ne
sont donc pas servis. Ouvert en C46 : c'est exactement le piège qui vient de se refermer avec
la FAQ, quelqu'un les rebranchera en croyant qu'ils décrivent l'offre courante.

Typecheck 0 erreur, lint 0 warning, 84 tests verts.

---

## Le déploiement, et ce qu'il a révélé

Déployé dans l'ordre imposé : commit des sources, `npm run build` (le garde-fou a confirmé
les 129 fichiers de `public/` conformes à HEAD), commit et **push du manifeste avant** Netlify,
déploiement, vérification des **209 entrées une par une** sur `ainspiration2026.netlify.app` —
l'URL exacte que le conteneur utilise, pas celle du site — puis `compose-exact` pour recréer
le conteneur.

### Le dépôt était propre, le site ne l'était pas

C'est en vérifiant le **contenu réellement servi** que le vrai reste est apparu : après le
premier déploiement, `/realisations` servait encore un bloc *« Paperclip — comité de direction
assisté »*, le sitemap annonçait `/realisations/paperclip` avec ses quatre `hreflang`, et
l'URL rendait **200**.

Retirer la fiche de `src/data/realisations.ts` le 25/09 n'avait touché qu'un endroit sur
quatre. Le slug vivait aussi dans :

| Où | Effet |
|---|---|
| `routes/seo.js`, `REALISATION_DETAIL_SLUGS` | **200** sur une page disparue |
| `scripts/vite-plugin-sitemap.ts` | l'URL annoncée aux moteurs |
| `public/locales/{fr,en,nl}/realisations.json` | **15 items pour 14 fiches**, et le texte servi |

Le plus instructif : `seo.js` porte en clair le commentaire *« KEEP IN SYNC with the
`format: 'complet'` entries in src/data/realisations.ts »*. **La consigne était écrite à
l'endroit exact où il fallait la lire, et je l'ai quand même manquée** — parce que j'ai
supprimé la fiche par une expression régulière sur un seul fichier au lieu de chercher le slug
dans tout le dépôt. Et le sitemap porte ses slugs **à la main**, un commentaire expliquant
pourquoi (le plugin tourne avant la compilation TypeScript) : aucune suppression de données ne
le mettra jamais à jour tout seul.

**La leçon : supprimer une entité de ce dépôt, c'est chercher son identifiant partout —
`grep -rn <slug> src public/locales docker/backend scripts` — jamais éditer la structure de
données qui la porte.** Et la vérification qui compte n'est pas « le dépôt ne le contient
plus », c'est « le site ne le sert plus ».

Après correction et second déploiement complet :

- Paperclip : **0 occurrence** sur `/realisations` dans les trois langues, dans le sitemap et
  dans `seo-routes.json`.
- `/realisations/paperclip` rend **404**, une fiche vivante rend **200**, un slug inventé rend
  **404**. Le comportement est celui qu'on veut, pas un 200 vide.
- `/realisations` sert **exactement 14 fiches** (17 `<h2>`, dont 3 titres de section).
- `band.count` = 14 et 14 items réels, dans les trois langues ; `/a-propos` dit Quatorze,
  Fourteen, Veertien.
- Les libellés corrigés sont servis : `animatedStats` ne porte plus `setup`, `video.json` ne
  promet plus de première vidéo en 48h, la meta de `/solutions` est sans délai.

`node scripts/health-check.mjs` : **0 échec, 0 avertissement, 27 contrôles passés** — dont
« sitemap : 88 URLs toutes en 200 » et « 115 liens internes, aucun mort ». CI vert sur les
trois commits.

---

## Audityo : une erreur de droit au cœur de l'argument

Audité **en production seulement** : la règle « une session par dépôt » interdit d'écrire dans
`Audityo/` depuis ici. Ce qui relève du code de ce dépôt est décrit dans
`docs/chantiers/audityo-irreprochable.md`, prêt à coller. Ce qui relève de n8n a été corrigé,
parce que n8n n'est pas un dépôt voisin.

### La phrase qui retourne l'argument contre lui

`audityo.eu` annonce, juste avant ses trois montants de sanctions :

> « Le montant retenu est **le plus élevé des deux** : la somme absolue, ou le pourcentage du
> chiffre d'affaires mondial. »

Puis 35 M€, 15 M€, 7,5 M€. C'est le régime général, et c'est exact — **pour une grande
entreprise**. Or le titre de la page est *« Conformité EU AI Act pour les PME européennes »*,
et pour une PME la règle est inversée :

> **Article 99 § 6** — « In the case of SMEs, including start-ups, each fine … shall be up to
> the percentages or amount referred to in paragraphs 3, 4 and 5, **whichever thereof is
> lower**. »

Vérifié sur `ai-act-service-desk.ec.europa.eu`, le service officiel de la Commission, et
concordant sur quatre autres sources juridiques. Je ne l'ai pas affirmé de mémoire : c'est
exactement le genre de point où la mémoire d'un modèle ne suffit pas.

**Ce que ça produit.** Pour une PME à 2 M€ de chiffre d'affaires, le plafond de non-conformité
est de **3 % de 2 M€, soit 60 000 €** — pas 15 millions. La page affiche à son propre public
une exposition fausse d'un facteur d'environ 250, et ça se lit comme de la vente par la peur.
Sur un produit de conformité, une erreur de droit dans le chiffre d'accroche coûte la
crédibilité entière : c'est la première chose qu'un juriste vérifiera.

### Et je l'avais recopiée dans le chatbot

Le prompt du chatbot Audityo, que j'ai écrit le 25/09, portait la même phrase — reprise
telle quelle de la page d'accueil. Le chatbot aurait donc annoncé 35 millions à une PME qui
demande ce qu'elle risque.

Corrigé dans n8n le 26/09, et **testé deux fois en production** :

- *« je suis une PME de 15 personnes, combien je risque ? »* → il explique la règle du plus bas
  des deux et **refuse de chiffrer**, faute de connaître le chiffre d'affaires.
- *« quelles sont les amendes prévues ? »* → il donne les trois paliers du régime général, puis
  la règle PME, puis la clause de non-conseil juridique.

C'est la limite de la méthode du 25/09 : j'avais construit le prompt « à partir de faits
relevés sur la page d'accueil, rien n'y a été ajouté ». Discipline utile contre l'invention —
**mais elle recopie fidèlement les erreurs de la source.** Un prompt bâti sur le site hérite
de ce que le site a de faux ; il faut vérifier les faits juridiques à la source officielle, pas
à la page marketing.

### Deux défauts de référencement

**Dix-huit articles, zéro dans le sitemap.** Il ne porte que 10 URLs. Le point rassurant : les
articles **sont bien rendus côté serveur**, ~8 300 caractères dans le HTML brut sans exécuter
de JavaScript. Ils n'ont donc pas le défaut qui a rendu le blog d'AInspiration invisible trois
mois — ils ne sont simplement pas annoncés. Le sitemap d'AInspiration en porte 88.

**Le sitemap annonce deux pages `noindex`** — `/auth/login` et `/auth/register`. Signal
contradictoire, remonté en erreur par Search Console. Le `noindex` est le bon choix ; c'est
l'annonce qui ne l'est pas.

### Ce qui est juste, et mérite d'être dit

Le reste tient, et mieux que la moyenne du secteur :

- **Le calendrier, report compris** : l'annexe III reportée au 2 décembre 2027 par l'omnibus,
  confirmé sur `digital-strategy.ec.europa.eu`. L'article de blog du 27/08 dit même
  explicitement que le 2 août 2026 « n'est plus tout à fait exact » et donne aussi le 2 août
  2028 pour l'annexe I.
- Les quatre catégories et leurs références — article 5, annexe III, article 50, considérant
  165. Exactes. Les trois montants de l'article 99 aussi.
- TVA BE 0462.122.648 sur les quatre pages légales, cohérente avec AInspiration.
- **Aucune preuve fabriquée** : pas de faux client, pas de témoignage, pas d'étude de cas
  imaginaire. L'accueil dit « rien n'est facturé à ce jour ».
- Pas de contenu fabriqué sur URL inconnue. Et le soft 404 vers `/auth/login` est déjà
  neutralisé pour l'indexation par un `noindex` **et** un `canonical` sans query string.

Deux affirmations restent à arbitrer, pas à corriger seul : « la même conformité est
aujourd'hui vendue à la journée par des cabinets » (comparaison sans source) et « satisfait ou
remboursé 30 jours » alors que rien n'est encore facturé.

Ouvert en **C47**.
